meta {
  name: user
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/auth/update-user
  body: json
  auth: inherit
}

body:json {
  {
    "name": "updatedName"
  }
}

settings {
  encodeUrl: true
}
