meta {
  name: create role
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/authz/roles
  body: json
  auth: inherit
}

body:json {
  {
    "organizationId": "{{devOrgId}}",
    "name": "Dev",
    "description": "Access to organization configuration"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
