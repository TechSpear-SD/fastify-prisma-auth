meta {
  name: create role
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/authz/roles
  body: json
  auth: inherit
}

body:json {
  {
    "organizationId": "{{globalOrgId}}",
    "name": "Visitor",
    "description": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
